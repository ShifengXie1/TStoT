# Core runtime dependencies
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.1
transformers>=4.40

# Optional utilities used by helper modules
optuna>=3.0
tabulate>=0.9

# If you need a specific CUDA build of PyTorch on Windows,
# install torch from the official PyTorch index instead:
# https://pytorch.org/get-started/locally/
